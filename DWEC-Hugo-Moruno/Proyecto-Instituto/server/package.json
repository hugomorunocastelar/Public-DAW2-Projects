{
  "dependencies": {
    "cors": "^2.8.5",
    "json-server": "^0.17.4",
    "json-server-auth": "^2.1.0",
    "multer": "^1.4.5-lts.1"
  },
  "scripts": {
    "stsrvimgs": "node server.js",
    "stjssrvauth": "npx json-server --watch db.json -m ./node_modules/json-server-auth -r routes.json",
    "start-both": "start npm run stsrvimgs && start npm run stjssrvauth && echo 'DB started on :3000 and IMG started on :3001'"
  }
}
